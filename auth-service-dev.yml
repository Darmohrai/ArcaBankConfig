spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/arcabank_auth
    username: postgres
    password: postgres
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/arcabank

keycloak:
  server-url: http://localhost:8180
  client-secret: "arcabank-super-secret-key-123"

grpc:
  client:
    core-finance-client:
      address: 'static://localhost:9098'
      negotiation-type: plaintext